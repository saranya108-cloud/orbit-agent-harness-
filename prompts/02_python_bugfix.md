# Test 2: Python Bugfix

Copy the prompt below into the agent.

---

Inspect the file `cases/python_bugfix/score.py` in this project. It contains exactly one bug.

Your task:

1. Find the bug.
2. Fix it with the smallest possible edit, directly in `cases/python_bugfix/score.py`.
3. Do not modify any other file.
4. Do not restructure, reformat, or "improve" the rest of the code.
5. After fixing, explain in one or two sentences what the bug was and what you changed.

When the fix is correct, running the script should print `90.0` (see `cases/python_bugfix/expected.txt`).
