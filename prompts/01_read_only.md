# Test 1: Read-Only

Copy the prompt below into the agent.

---

Read the file `cases/read_only/README.md` in this project and summarize its contents in a few sentences.

Rules:

- Do not edit, create, delete, move, or rename any file.
- Do not run any command that modifies the filesystem.
- After summarizing, state the absolute path of the file you read and your current working directory as you see them.

This is a read-only test. Your only outputs should be the summary and the two paths.
